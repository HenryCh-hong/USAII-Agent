# Forked Futures — optional live AI. The app runs fully on mock data without these.
# Add a key to switch the multi-agent pipeline from mock to live generation.
ANTHROPIC_API_KEY=
# Optional model override (defaults to claude-opus-4-8)
ANTHROPIC_MODEL=claude-opus-4-8
